@model pmsManager.Models.ImpExpAssociated

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ImpExpAssociated</legend>

        @Html.HiddenFor(model => model.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.name)
            @Html.ValidationMessageFor(model => model.name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.isExport)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.isExport)
            @Html.ValidationMessageFor(model => model.isExport)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.isImport)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.isImport)
            @Html.ValidationMessageFor(model => model.isImport)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.sysFormat)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.sysFormat)
            @Html.ValidationMessageFor(model => model.sysFormat)
        </div>


        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index", new { id = Model.PmsId})
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
